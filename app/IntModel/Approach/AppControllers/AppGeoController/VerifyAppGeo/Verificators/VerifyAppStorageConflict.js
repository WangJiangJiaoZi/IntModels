
import sharedErrors from "../../../../../SharedErrors/SharedErrors";
//import initSettings from "../../../../../EnvSetters/InitSettings";

const verifyAppStorageConflict = (storageLength, slipLength, appId) => {
	/*
	const SingleConflictError = sharedErrors.SingleConflictError;
	const sharedInitSettings = initSettings.sharedInitSettings;
	const intRadius = sharedInitSettings.intDiameter / 2; //in meters
	const modelOwner = "Approach";

	if (storageLength + slipLength >= intRadius * 0.9) {
		const message = "approach " + appId + "inbound storage too long, please reduce its length.";
		const oneSingleConflictError = new SingleConflictError(modelOwner, message);
		throw oneSingleConflictError;
	}
	*/
};

export default verifyAppStorageConflict;
